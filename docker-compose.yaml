version: "3.9"

services:

  django-app:
    build: .
    container_name: 'django_app'
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - '8000:8000'
    depends_on:
      - pgdb



  pgdb:
    image: postgres
    ports:
      - '5432:5432'
    container_name: pgdb





